# v1.0.0 GitHub Release Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** mcp-server-steam 프로젝트의 첫 번째 공식 릴리스(v1.0.0)를 GitHub에 생성하고 배포

**Architecture:** Git 태그 생성 → 소스 코드 압축 → GitHub Release 생성 순서로 진행. GitHub CLI(gh)와 Git 명령어를 사용하여 수동으로 릴리스 생성.

**Tech Stack:** Git, GitHub CLI (gh), bash

---

### Task 1: Git 상태 확인 및 정리

**Files:**
- None (작업 디렉토리 상태만 확인)

**Step 1: Git 상태 확인**

```bash
git status
```

Expected output: "working tree clean" 또는 staged/unstaged changes 없음
만약 변경사항이 있다면:
```bash
# 변경사항을 커밋하거나 stash
git stash  # 임시 저장 (필요시)
```

**Step 2: 현재 브랜치 확인**

```bash
git branch --show-current
```

Expected output: `main`

---

### Task 2: Git 태그 생성

**Files:**
- Git repository (tag 생성)

**Step 1: 태그 생성**

```bash
git tag -a v1.0.0 -m "Release v1.0.0: 첫 번째 공식 릴리스"
```

Expected output: (아무것도 없음 - 성공)

**Step 2: 태그 생성 확인**

```bash
git tag -l "v1.0.0"
```

Expected output:
```
v1.0.0
```

**Step 3: 태그 상세 정보 확인**

```bash
git show v1.0.0
```

Expected output: 태그 정보와 커밋 내용 출력

---

### Task 3: 태그를 GitHub에 푸시

**Files:**
- None (remote git repository)

**Step 1: 태그 푸시**

```bash
git push origin v1.0.0
```

Expected output:
```
To github.com:YOUR_USERNAME/mcp-server-steam.git
 * [new tag]         v1.0.0 -> v1.0.0
```

**Step 2: GitHub에서 태그 확인**

```bash
gh release list
```

Expected output: v1.0.0 태그가 보임 (릴리스는 아직 생성되지 않음)

---

### Task 4: 소스 코드 압축 파일 생성 (tar.gz)

**Files:**
- Create: `mcp-server-steam-v1.0.0.tar.gz`

**Step 1: tar.gz 압축 파일 생성**

```bash
git archive --format=tar.gz --prefix=mcp-server-steam-v1.0.0/ v1.0.0 > mcp-server-steam-v1.0.0.tar.gz
```

Expected output: (아무것도 없음 - 성공)

**Step 2: 파일 생성 확인**

```bash
ls -lh mcp-server-steam-v1.0.0.tar.gz
```

Expected output:
```
-rw-r--r-- 1 user group 50K Feb 24 12:00 mcp-server-steam-v1.0.0.tar.gz
```
(파일 크기는 실제와 다를 수 있음)

**Step 3: 압축 내용 확인 (선택사항)**

```bash
tar -tzf mcp-server-steam-v1.0.0.tar.gz | head -20
```

Expected output: 소스 코드 파일 목록이 출력됨

---

### Task 5: 소스 코드 압축 파일 생성 (zip)

**Files:**
- Create: `mcp-server-steam-v1.0.0.zip`

**Step 1: zip 압축 파일 생성**

```bash
git archive --format=zip --prefix=mcp-server-steam-v1.0.0/ -o mcp-server-steam-v1.0.0.zip v1.0.0
```

Expected output: (아무것도 없음 - 성공)

**Step 2: 파일 생성 확인**

```bash
ls -lh mcp-server-steam-v1.0.0.zip
```

Expected output:
```
-rw-r--r-- 1 user group 55K Feb 24 12:00 mcp-server-steam-v1.0.0.zip
```

**Step 3: 압축 내용 확인 (선택사항)**

```bash
unzip -l mcp-server-steam-v1.0.0.zip | head -20
```

Expected output: 소스 코드 파일 목록이 출력됨

---

### Task 6: 릴리스 노트 작성

**Files:**
- Create: `RELEASE_NOTES.md` (임시)

**Step 1: 릴리스 노트 파일 생성**

다음 내용으로 `RELEASE_NOTES.md` 파일 생성:

```markdown
# 🎮 mcp-server-steam v1.0.0

FastMCP 3.0 beta2를 사용하여 Steam Web API와 통합하는 종합 MCP(Model Context Protocol) 서버의 첫 번째 공식 릴리스입니다.

## ✨ 주요 기능

### 사용자 프로필 및 통계
- 프로필 정보 (닉네임, 아바타, 계정 상세)
- 친구 목록 조회
- 소유 게임 라이브러리 전체 조회
- 플레이시간 데이터 분석
- 최근 플레이한 게임 목록
- Steam 레벨 확인
- 업적 진행상황 조회

### 게임 정보 및 스토어
- 게임 상세 정보 및 메타데이터
- 스토어 가격 및 할인 정보 확인
- 게임 뉴스 및 업데이트 확인
- Steam에서 게임 검색
- 특정 게임의 업적 데이터 및 스키마
- 전체 플레이어 업적 달성률 통계

### 커뮤니티 기능
- Steam 워크샵 아이템 조회 및 상세 정보
- 사용자 리뷰 확인
- VAC 및 게임 밴 상태 조회

### 유틸리티
- 커스텀 프로필 URL(Vanity URL)을 Steam ID로 변환

## 📦 설치 방법

### 1. 저장소 클론

\`\`\`bash
git clone https://github.com/YOUR_USERNAME/mcp-server-steam.git
cd mcp-server-steam
\`\`\`

### 2. uv로 의존성 설치

\`\`\`bash
uv sync
\`\`\`

### 3. Steam Web API 키 발급

- https://steamcommunity.com/dev/apikey 방문
- Steam 계정으로 로그인
- 도메인 등록 후 API 키 발급

### 4. 환경 설정

\`\`\`bash
cp .env.example .env
\`\`\`

`.env` 파일에 API 키 추가:
\`\`\`
STEAM_API_KEY=your_api_key_here
\`\`\`

## 🚀 사용 방법

### Claude Desktop 설정

\`\`\`json
{
  "mcpServers": {
    "steam": {
      "command": "uv",
      "args": ["run", "python", "/path/to/mcp-server-steam/server.py"],
      "env": {
        "STEAM_API_KEY": "your_api_key_here"
      }
    }
  }
}
\`\`\`

서버 실행:
\`\`\`bash
uv run python server.py
\`\`\`

## 🛠️ 사용 가능한 도구

### 프로필 도구
- \`get_user_profile\` - Steam 사용자 프로필 조회
- \`get_friends_list\` - 친구 목록 조회
- \`get_owned_games\` - 소유한 모든 게임 조회
- \`get_recently_played_games\` - 최근 플레이한 게임 조회
- \`get_steam_level\` - Steam 레벨 조회
- \`get_player_achievements\` - 특정 게임의 업적 진행상황 조회

### 게임 도구
- \`get_game_details\` - 스토어에서 게임 정보 조회
- \`get_game_news\` - 게임 뉴스 및 업데이트 조회
- \`get_global_achievement_percentages\` - 전체 업적 통계 조회
- \`search_games\` - Steam에서 게임 검색
- \`get_game_schema\` - 업적 및 통계 스키마 조회

### 커뮤니티 도구
- \`get_workshop_items\` - Steam 워크샵 아이템 조회
- \`get_workshop_item_details\` - 워크샵 아이템 상세 정보
- \`get_user_reviews\` - 게임 사용자 리뷰 조회
- \`get_player_bans\` - VAC 및 게임 밴 상태 조회

### 유틸리티 도구
- \`resolve_vanity_url\` - Vanity URL을 Steam ID로 변환

## 📋 요구 사항

- Python 3.10 이상
- FastMCP 3.0 beta2
- uv (Python 패키지 관리자)
- Steam Web API 키

## 🔄 주요 변경사항

### v1.0.0 (2026-02-24)
- FastMCP 3.0 beta2 기반 MCP 서버 구현
- Steam Web API 전체 통합
- 사용자 프로필 및 게임 라이브러리 지원
- 업적 및 통계 추적 기능
- Steam 워크샵 통합
- 속도 제한(Rate Limiting) 구현
- 한국어 전체 문서화

## 🐛 알려진 문제

- Steam API 속도 제한: 분당 100회 요청 제한 (자동 백오프 구현됨)

## 🔗 참고 링크

- [Steam Web API 문서](https://steamapi.xpaw.me/)
- [FastMCP 문서](https://gofastmcp.com/)
- [GitHub Repository](https://github.com/YOUR_USERNAME/mcp-server-steam)
- [이슈 트래커](https://github.com/YOUR_USERNAME/mcp-server-steam/issues)

## 📄 라이선스

MIT License - 자세한 내용은 LICENSE 파일을 참조하세요

---

**다운로드:** 이 릴리스에 포함된 소스 코드 아카이브(tar.gz, zip)를 다운로드하여 사용하세요.
```

**Step 2: 파일 생성 확인**

```bash
cat RELEASE_NOTES.md | head -50
```

Expected output: 릴리스 노트 내용의 처음 50줄이 출력됨

---

### Task 7: GitHub 릴리스 생성

**Files:**
- None (GitHub release)

**Step 1: 릴리스 생성 명령어 실행**

```bash
gh release create v1.0.0 \
  --title "v1.0.0: 첫 번째 공식 릴리스" \
  --notes-file RELEASE_NOTES.md \
  mcp-server-steam-v1.0.0.tar.gz \
  mcp-server-steam-v1.0.0.zip
```

Expected output:
```
https://github.com/YOUR_USERNAME/mcp-server-steam/releases/tag/v1.0.0
```

**Step 2: 릴리스 생성 확인**

```bash
gh release view v1.0.0
```

Expected output: 릴리스 상세 정보가 출력됨 (태그, 제목, 설명, 에셋)

**Step 3: 브라우저에서 확인 (선택사항)**

```bash
gh release view v1.0.0 --web
```

Expected output: 브라우저가 열리고 GitHub 릴리스 페이지가 표시됨

---

### Task 8: 정리 및 검증

**Files:**
- Delete: `RELEASE_NOTES.md`
- Delete: `mcp-server-steam-v1.0.0.tar.gz`
- Delete: `mcp-server-steam-v1.0.0.zip`

**Step 1: 임시 파일 삭제**

```bash
rm RELEASE_NOTES.md mcp-server-steam-v1.0.0.tar.gz mcp-server-steam-v1.0.0.zip
```

Expected output: (아무것도 없음)

**Step 2: 삭제 확인**

```bash
ls -la | grep -E "(RELEASE_NOTES|mcp-server-steam-v1.0.0)"
```

Expected output: (아무것도 없음 - 파일이 삭제됨)

**Step 3: 최종 Git 상태 확인**

```bash
git status
```

Expected output:
```
On branch main
Your branch is up to date with 'origin/main'.

nothing to commit, working tree clean
```

**Step 4: 원격 태그 확인**

```bash
git ls-remote --tags origin
```

Expected output:
```
...	refs/tags/v1.0.0
```

---

### Task 9: 최종 릴리스 검증

**Files:**
- None

**Step 1: 릴리스 목록 확인**

```bash
gh release list --limit 5
```

Expected output:
```
v1.0.0  첫 번째 공식 릴리스  Latest  2026-02-24
```

**Step 2: 릴리스 에셋 확인**

```bash
gh release view v1.0.0 --json assets --jq '.assets[].name'
```

Expected output:
```
mcp-server-steam-v1.0.0.tar.gz
mcp-server-steam-v1.0.0.zip
```

**Step 3: 다운로드 테스트 (선택사항)**

```bash
gh release download v1.0.0 --pattern "*.tar.gz" --dir /tmp
```

Expected output: 파일이 /tmp로 다운로드됨

**Step 4: 다운로드한 파일 삭제**

```bash
rm /tmp/mcp-server-steam-v1.0.0.tar.gz
```

---

## 완료 기준

✅ Git 태그 v1.0.0이 로컬 및 원격에 생성됨
✅ GitHub 릴리스 v1.0.0이 생성됨
✅ 릴리스 노트가 상세 한국어로 작성됨
✅ 소스 코드 압축 파일 2개(.tar.gz, .zip)가 첨부됨
✅ 릴리스가 "Latest"로 표시됨
✅ 모든 임시 파일이 삭제됨
✅ Working directory가 깨끗한 상태

## 문제 해결

### 태그가 이미 존재하는 경우

```bash
# 기존 태그 삭제 (로컬)
git tag -d v1.0.0
# 기존 태그 삭제 (원격)
git push origin :refs/tags/v1.0.0
# 다시 생성
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin v1.0.0
```

### gh 인증 오류

```bash
gh auth login
```

### 릴리스 수정이 필요한 경우

```bash
# 기존 릴리스 삭제
gh release delete v1.0.0 --yes
# 태그도 삭제하고 다시 시작
git push origin :refs/tags/v1.0.0
# 처음부터 다시 시작
```
